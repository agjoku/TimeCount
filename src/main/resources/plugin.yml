name: CommandTestPlugin
version: '${project.version}'
main: com.github.agjoku.bukkit.commandtestplugin.CommandTestPlugin
api-version: 1.18

commands:
  timecount:
    description: Time count
    usage: /<command> [count]
